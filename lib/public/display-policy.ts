/**
 * Temporary public display policy.
 * Prices and EVFAKTA scores remain in the database for admin/future use,
 * but stay hidden from public UI until launch-ready.
 */
export const PUBLIC_SHOW_PRICES = false;
export const PUBLIC_SHOW_SCORES = false;
